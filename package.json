{
  "name": "figma-api-client",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "format": "oxfmt",
    "format:check": "oxfmt --check",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "pnpm -r run release",
    "prepare": "husky --filter @plainbrew/figma-api-client && pnpm run generate:schema"
  },
  "devDependencies": {
    "@changesets/cli": "^2.30.0",
    "@changesets/config": "^3.1.3",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "oxfmt": "^0.36.0"
  },
  "packageManager": "pnpm@10.24.0"
}
